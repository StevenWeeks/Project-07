import React from 'react'
import Nav from './Nav';

// imports the Nav.js info, and puts it in a div.


const Header = (props) => {
    return (
      <div>

        <Nav />
      </div>

    );
}

export default Header;
